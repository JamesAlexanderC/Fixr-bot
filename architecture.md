# Need to work around so shit proxies

## Scan loop

Using Camoufox + standard mobile/resiential proxies works fine as expected

This is the main part where rate limiting / IP ratings become an issue, so decent rotating proxies + exponential backoff retries should be perfect

## Get ticket 

Camoufox + mobile proxies work 99% of the time perfectly

I'm thinking just the same here, maybe could speed it up with some self implemented proxies but idk

# Buy ticket

This is the problem stripe are much more trigger happy mass banning IP's and I haven't yet found a proxy provider good enough that doesn't restrict usage to fixr

I think for this final stage (or maybe get + buy tickets) I am going to have to implement a homemade proxy - will have to use sparingly to maintain IP reputation, but
I can think of ways to use mobile data and the like to basically just end up using a personal mobile proxy, but organically harvested which will significantly improve success rates

